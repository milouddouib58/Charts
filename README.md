# نظام التقييم الشامل المطور (v3.0)

تطبيق ويب تفاعلي لتقييم مهارات التلاميذ وتشخيص مستوى استعدادهم للمرحلة الابتدائية. تم تطوير هذا الإصدار ليشمل تحسينات جمالية ووظيفية وهيكلية.

## الإصدار 4.0 - المميزات الشاملة 🌟
تم ترقية النظام ليشمل أدوات متقدمة للتحليل والمتابعة:

1. **لوحة تحكم القسم (Class Analytics)**:
   - رسوم بيانية لتوزيع مستويات التلاميذ.
   - تحديد المهارات الأكثر صعوبة على مستوى القسم.
   - ترتيب التلاميذ حسب الأداء.

2. **السجل الزمني (Progress Tracking)**:
   - يتم حفظ كل تقييم في سجل تاريخي.
   - رسم بياني يوضح تطور التلميذ بين التقييمات المختلفة.

3. **تقارير PDF احترافية**:
   - إمكانية تحميل تقرير شامل لكل تلميذ.
   - *ملاحظة:* لضمان ظهور اللغة العربية بشكل صحيح في PDF، يفضل وضع ملف خط (مثل Arial أو Tajawal) في مجلد `assets/fonts`.

## متطلبات التشغيل

تأكد من تثبيت Python 3.8 أو أحدث.

### تثبيت المكتبات
قم بتشغيل الأمر التالي في سطر الأوامر (Terminal):
```bash
pip install -r requirements.txt
```
(تم إضافة مكتبة `fpdf2` لدعم تقارير PDF)

## تشغيل التطبيق

لبدء تشغيل النظام، استخدم الأمر:
```bash
streamlit run app.py
```
سيعمل التطبيق وتفتح صفحة الويب تلقائياً. 🚀

## الاختبار والتطوير
تم إضافة اختبارات آلية للتحقق من سلامة البيانات. لتشغيلها:
```bash
python -m unittest tests/test_data_manager.py
```
